name: 'Debug Workflow'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello, World! If you can see this, the repository can run workflows." 